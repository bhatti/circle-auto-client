version: 2.1
orbs:
  sectest: bhatti/sectest@dev
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk
    steps:
      - persist_to_workspace:
          root: /tmp/workspace
          paths:
            - nowsecure-auto-security-test/*
      - attach_workspace:
        at: /tmp/workspace
      - checkout
      - cp apkpure_app_887.apk /tmp/test.apk
      - sectest/sectest:
        auto_file: test.apk
